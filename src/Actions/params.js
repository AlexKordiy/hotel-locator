import { createAction } from 'redux-act';

export const SET_CURRENCY = createAction('SET_CURRENCY');
export const SET_CITY = createAction('SET_CITY');
export const SET_CHECK_IN = createAction('SET_CHECK_IN');
export const SET_CHECK_OUT = createAction('SET_CHECK_OUT');
export const SET_RADIUS = createAction('SET_RADIUS');
export const SET_MAX_RATE = createAction('SET_MAX_RATE');

export const VALIDATE = createAction('VALIDATE');
export const VERIFY = createAction('VERIFY');

export const TOGGLE_SPINER = createAction('TOGGLE_SPINER');

