import { createAction } from 'redux-act';

export const FETCH_DATA = createAction('FETCH_DATA');
export const SET_PAGE = createAction('SET_PAGE');
export const SET_ROWS_PER_PAGE = createAction('SET_ROWS_PER_PAGE');
